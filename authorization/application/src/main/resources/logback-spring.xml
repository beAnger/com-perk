<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false">
    <springProperty scope="context" name="application" source="spring.application.name"/>
    <springProperty scope="context" name="profile" source="spring.profiles.active"/>
    <property name="LOG_PATH" value="./logs" />
    <property name="LOG_CHARSET" value="utf-8" />
    <property name="LOG_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>
    <property name="LOG_PATTERN_COLOR"
              value="%red(%d{yyyy-MM-dd HH:mm:ss.SSS}) %green([%thread]) %highlight(%-5level) %boldMagenta(%logger{36}) - %highlight(%msg%n)"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="pro.yqy.component.web.logback.MaskPatternLayout">
                <pattern>${LOG_PATTERN_COLOR}</pattern>
            </layout>
            <charset>${LOG_CHARSET}</charset>
        </encoder>
    </appender>

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <FileNamePattern>${LOG_PATH}/${application}.%d{yyyy-MM-dd}.%i.log</FileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>4GB</totalSizeCap>
        </rollingPolicy>
        <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
            <layout class="pro.yqy.component.web.logback.MaskPatternLayout">
                <pattern>${LOG_PATTERN}</pattern>
            </layout>
            <charset>${LOG_CHARSET}</charset>
        </encoder>
    </appender>

    <logger name="org.hibernate.validator" level="INFO"/>
    <logger name="org.apache.dubbo" level="INFO"/>
    <logger name="com.alibaba.nacos" level="INFO"/>
    <logger name="org.springframework" level="INFO"/>

    <!-- 开发环境日志配置 -->
    <springProfile name="dev">
        <logger name="root" level="INFO">
            <appender-ref ref="STDOUT"/>
            <appender-ref ref="FILE"/>
        </logger>
    </springProfile>

    <!-- 生产环境日志配置 -->
    <springProfile name="prod">
        <logger name="root" level="INFO">
            <appender-ref ref="FILE"/>
        </logger>
    </springProfile>
</configuration>
